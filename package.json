{
  "name": "bantang-pwa",
  "version": "1.3.0",
  "private": true,
  "description": "半糖主义 PWA — 咖啡与奶茶饮品记录",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build && node -e \"require('fs').writeFileSync('dist/.nojekyll','')\"",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@capacitor/android": "^8.4.2",
    "@capacitor/cli": "^8.4.2",
    "@capacitor/core": "^8.4.2",
    "typescript": "^5.7.0",
    "vite": "^6.0.0"
  },
  "dependencies": {
    "@imgly/background-removal": "^1.6.0",
    "onnxruntime-web": "1.21.0"
  }
}
